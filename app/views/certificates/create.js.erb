$('#certificate_list').find('.certificates-list-wrapper').html("<%= j render(@certificates) %>");
$('#certificate-modal').modal('hide');

document.getElementsByClassName('new_certificate')[0].reset()
